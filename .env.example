# Tiger Cloud connection. Use the least-privilege app_api role created by migration 003.
DATABASE_URL=postgresql://app_api:CHANGE_ME@HOST:PORT/DATABASE?sslmode=require

# Nessie API customer key.
API_KEY=

# Generate with: cd backend && python3 -c 'import crypto; print(crypto.generate_key_b64())'
CFDI_ENC_KEY=
